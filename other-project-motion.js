(()=>{const prepare=()=>{const track=document.querySelector('.other-track');if(!track||track.dataset.marquee)return false;const cards=[...track.querySelectorAll('.other-card')];if(!cards.length)return false;const markup=cards.map(card=>card.outerHTML).join('');track.innerHTML=`<div class="other-rail">${markup}${markup}</div>`;track.dataset.marquee='true';return true};let attempts=0;const timer=setInterval(()=>{attempts+=1;if(prepare()||attempts>100)clearInterval(timer)},50)})();
